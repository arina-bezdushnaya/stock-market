import {observer} from 'mobx-react';
import {CompaniesContainer} from './styled';

export const CompanyPrice = observer(() => {

  // const averageStock = (open + close) / 2;

  return <CompaniesContainer></CompaniesContainer>;
});
